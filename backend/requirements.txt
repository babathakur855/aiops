fastapi==0.115.0
uvicorn[standard]==0.30.6
anthropic>=0.40.0
pydantic>=2.7.0
pydantic-settings>=2.3.0
httpx>=0.27.0
python-dotenv>=1.0.0
structlog>=24.1.0
websockets>=13.0
python-multipart>=0.0.9

# Kubernetes (optional — app works without a cluster using mock data)
kubernetes>=30.1.0

# Auth
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.2.1             # passlib incompatible with bcrypt 5.x

# LLM providers (optional — install based on LLM_PROVIDER)
openai>=1.30.0          # azure_openai provider + Azure OpenAI connector
boto3>=1.34.0           # aws_bedrock provider + AWS connector

# Cloud provider connectors (install only what you use)
# AWS (boto3 above covers this)
# Azure
azure-identity>=1.16.0
azure-mgmt-resource>=23.0.0
azure-mgmt-costmanagement>=4.0.0
azure-mgmt-containerservice>=31.0.0
azure-mgmt-monitor>=6.0.0
azure-monitor-query>=1.3.0
# GCP
google-auth>=2.29.0
google-cloud-resource-manager>=1.12.0
google-cloud-monitoring>=2.21.0
google-cloud-logging>=3.10.0
google-cloud-container>=2.49.0
google-cloud-billing>=1.13.0
google-cloud-aiplatform>=1.57.0
# ollama uses httpx (already included)
